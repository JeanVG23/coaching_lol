requests>=2.32
zstandard>=0.23
pydantic>=2
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
# Rang ML estimé (web/backend/ml_rank.py) : xgb+rf uniquement (pas l'EBM, ni
# `interpret`/`shap` qui restent des outils d'analyse hors-ligne) pour limiter
# le poids de l'image de prod.
numpy>=2.0
pandas>=2.0
scikit-learn>=1.5
xgboost>=2.1
